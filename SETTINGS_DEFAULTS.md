# 设置项默认值一览

> 本文件由代码整理而来（`XSpiderMac/Sources/XSpiderMac/Models/Settings.swift`），
> 用于快速核对"出厂默认是什么"。
> **改默认值时请同步更新本文档**——它是用户与 agent 共同的参照。

说明：
- 「范围」列给出代码里的钳制区间（超出会被夹到边界，不是报错）。
- 标 **nil 安全** 的项表示：设置里没存过该键时用"默认值"列的值
  （因此老版本升级后行为不会突变）。

---

## 账户与网络

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 代理开关 | 跟随系统（`useSystem = true`） | — | 关闭则不走代理 |
| 代理地址 | `http://127.0.0.1:7890` | — | 手动模式下使用 |
| 代理用户名 / 密码 | 空 | — | 可选，支持需要认证的代理 |

## 请求限流（X API）

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 请求闸门 | **开**（nil 安全） | — | 按端点分类排队 + 令牌桶限速 |
| 时间窗内请求数 | 100 | 1–600 | 令牌桶容量 |
| 时间窗秒数 | 10 | 1–300 | 与上一项共同决定速率 |
| 同端点串行 | **开**（nil 安全） | — | 前一请求完成前不发下一个 |
| 429 熔断 | **开** | — | 触发后暂停该类端点，避免越限越试 |
| 熔断冷却 | 300 秒 | 30–3600 | |

## 媒体 CDN 限流（下载侧，与 API 配额独立）

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| CDN 限流时降低并发 | **开** | — | |
| CDN 限流时并发上限 | 1 | 1–10 | |
| CDN 限流暂停 | 120 秒 | 10–3600 | |

## 下载

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 保存路径 | 系统「下载」目录 | — | 首次启动时写入 |
| 文件名模板 | `%POST_TIME% %USER_SCREEN_NAME% %POST_ID% %EXT%` | — | |
| 跳过已下载文件 | **开** | — | |
| 判定依据 | **按文件名**（`fileName`） | 文件名 / 记录文件 | 记录文件模式改文件名也不失效 |
| 记录文件名 | `.downloaded.json` | — | 记录文件模式用 |
| 按账号建子文件夹 | **开**（nil 安全） | — | 存到「昵称-@用户名」目录 |
| 自动加载媒体 | **开**（nil 安全） | — | 关闭后需手动点「加载媒体」 |
| 下载引擎 | **aria2Next** | aria2Next / 内置 | |
| 同时下载文件数 | 5 | 1–20 | |
| 单文件最大连接数 | 6 | 1–256 | aria2Next 的 `stream-max-connections` |
| 文件分配方式 | `none` | none / prealloc / falloc | |
| 引擎自动切换阈值 | 5 MB | 1–2048 | 按文件大小选引擎 |
| aria2 RPC 端口 | 6801 | 1024–65535 | |
| 端口策略 | 固定（`fixed`） | 固定 / 随机 | |
| 显示下载提示框 | **开**（nil 安全） | — | 右下角悬浮进度条 |

## 主页

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 数据源（搜索页） | **推文** | 推文 / 媒体 | 按用户记忆，无记录时用此默认 |
| 加快搜索页加载 | **开**（nil 安全） | — | 有日期范围时走 X 搜索端点 |
| 主页时间线 | 推荐（判断为 `forYou`） | 推荐 / 关注 | 持久化在独立的 UserDefaults 键 |
| 关注页排序 | 热门 | 热门 / 最新 | 同上 |
| 展示形态 | 推文卡片 | 推文 / 媒体 | 同上 |

## 状态检测

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 主动检测连接状态 | **开**（nil 安全） | — | 关闭后只被动采集（不发额外请求） |
| 检测间隔 | 30 秒 | **5**–3600 | 低于 5 秒会被 X 视为异常流量 |

## 翻译

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 自动翻译 | **关** | — | 开启后按语言清单自动翻译 |
| 自动翻译的语言 | **空**（不翻译任何条目） | — | 白名单；空 = 全不翻 |
| 翻译目标语言 | 跟随系统 | — | 解析用 `Settings.systemPreferredLanguage`（**不是** `Locale.current`） |

## 缓存

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 启用图片缓存 | **开**（nil 安全） | — | |
| 头像 / 媒体缩略图分类 | 各自 **开**（nil 安全） | — | 分类开关存独立 UserDefaults 键 |
| 缓存上限 | 200 MB | 50–500 | **十进制 MB**（与显示口径一致） |
| 超限回收比例 | 30 % | 10–100 | 达到上限时一次清掉已用容量的百分比 |
| 液态玻璃模糊强度 | 60 | 20–100 | 仅 macOS 26+ 生效 |

## 同步

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 启动时自动同步 | **关** | — | |
| 同步完成后退出应用 | **关** | — | |
| 同步页布局 | Dock（`dock`） | Dock / 蜂窝 | |
| 同步判定依据 | **同步记录文件** | 文件名 / 记录文件 | 记录文件可加速二次同步 |

## 外观与隐私

| 设置项 | 默认值 | 范围 | 说明 |
|---|---|---|---|
| 界面语言 | 简体中文 | 简中 / 繁中 / 英文 / 跟随系统 | |
| 界面字号 | 14 | 12–18 | |
| 液态玻璃外观 | **开**（nil 安全） | — | 仅 macOS 26+ 生效 |
| 下载时防止休眠 | **开** | — | 用系统电源断言，不改系统设置 |
| 自动删除下载历史 | **关** | — | 仅删记录，不删文件 |
| 自动删除搜索历史 | **关** | — | |
| 写日志到文件 | **关** | — | |

---

## 不随设置保存的"内在默认值"

这些是代码里的常量，不在设置界面暴露，改它们要动代码：

| 项 | 值 | 位置 |
|---|---|---|
| 分页每页条数 | 20 | `TwitterAPI` 各 `get*` 的 `count` |
| 页间节流 | 400–500 ms | `HomepageStore.runFillLoop` / `CreationTaskStore.pageThrottle` |
| 爬虫连续空页上限 | 5 页 | `CreationTaskStore.maxConsecutiveEmptyPages` |
| 批量建任务节流阈值 | > 50 条时分批，每批 25 | `DownloadStore.batchThrottleThreshold` |
| 缩略图解码尺寸 | 网格 600 / 详情 1600 / 查看器 4096 px | 各调用点 |
| 图片缓存磁盘扫描节流 | 60 秒至多一次 | `ImageCache.scheduleDiskLimitCheck` |
| 推文详情缓存 | TTL 5 分钟、容量 12 条 | `Support/TweetDetailCache.swift` |
| 关注状态缓存 | 300 秒 | `TwitterAPI.followCacheTTL` |
| 网络重试 | 最多 4 次、总预算 25 秒 | `NetworkClient` |
| 合成器"熔断中"兜底窗口 | 60 秒 | `AccountStatusStore.fallbackRateLimitedWindow` |
| 网络类异常 TTL | 30 秒 | `AccountStatusStore.networkErrorTTL` |
| 语言包下载等待上限 | 300 秒 | `TranslationPackStore.downloadTimeout` |
